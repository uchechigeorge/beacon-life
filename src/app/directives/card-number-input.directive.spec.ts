import { CardNumberInputDirective } from './card-number-input.directive';

describe('CardNumberInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CardNumberInputDirective();
    expect(directive).toBeTruthy();
  });
});
